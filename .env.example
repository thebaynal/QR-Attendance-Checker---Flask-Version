# Required — change this to a long random string in Azure!
SECRET_KEY=change-me-to-a-long-random-string

# Database path (use /home/data/ on Azure for persistence)
DB_PATH=/home/data/mascan_attendance.db

# Session files path
SESSION_FILE_DIR=/home/flask_session
